Program receives three real numbers on the input – that is the lengths of the sides
of the triangle, and returns the answer as the output values: "isosceles", "equilateral",
"rectangular", "arbitrary", "degenerate", "not a triangle", "incorrect input data".